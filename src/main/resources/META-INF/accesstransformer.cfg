# 26.1: sprite sets moved from ParticleEngine to ParticleResources (both private now).
# Used by SpriteMaterial (sprite lookup + selector list) and PhotonParticleManager.
public net.minecraft.client.particle.ParticleEngine resourceManager
public net.minecraft.client.particle.ParticleResources spriteSets
public net.minecraft.client.particle.ParticleResources$MutableSpriteSet

# RGBA16F workaround: blaze3d TextureFormat has no float color formats yet — Photon allocates the
# bloom chain's storage as GL_RGBA16F directly and wraps it in a GlTexture (declared RGBA8; the
# abstraction only reads the format for aspect checks). Drop when the engine grows float formats.
public com.mojang.blaze3d.opengl.GlTexture <init>(ILjava/lang/String;Lcom/mojang/blaze3d/textures/TextureFormat;IIIII)V
public com.mojang.blaze3d.opengl.GlBuffer handle
